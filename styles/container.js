import styled from "styled-components";

export const Container = styled.section`
  max-width: 1200px;
  padding: 30px;
  margin: 2rem auto;
  .section-tag {
    color: var(--color-mint);
    font-weight: bold;
    margin-bottom: 0;
  }
  .section-title {
    margin: 0;
    margin-bottom: 2rem;
    font-size: 3rem;
  }
`;
